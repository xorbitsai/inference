# Filebeat -> Elasticsearch 示例配置
# 用途: 采集 xinference supervisor/worker 应用日志 + 审计日志，直接写入 ES
# 使用: filebeat -e -c filebeat-to-es.yml
#
# 日志格式: JSON（每行一个 JSON 对象）
# 字段: @timestamp, level, module, process, role, address, node, message, exception

filebeat.inputs:
  # --- 应用日志（supervisor）---
  - type: log
    id: xinference-supervisor
    enabled: true
    paths:
      - /data/xinference_home/logs/xinference.log
      - /data/xinference_home/logs/xinference-*.log
    json.keys_under_root: true
    json.overwrite_keys: true
    json.add_error_key: true
    fields:
      log_type: supervisor
      cluster: xinference
    fields_under_root: true

  # --- 应用日志（worker）---
  - type: log
    id: xinference-worker
    enabled: true
    paths:
      - /data/xinference_home_worker/logs/xinference.log
      - /data/xinference_home_worker/logs/xinference-*.log
    json.keys_under_root: true
    json.overwrite_keys: true
    json.add_error_key: true
    fields:
      log_type: worker
      cluster: xinference
    fields_under_root: true

  # --- 审计日志（仅 supervisor）---
  - type: log
    id: xinference-audit
    enabled: true
    paths:
      - /data/xinference_home/logs/audit.log
    json.keys_under_root: true
    json.overwrite_keys: true
    json.add_error_key: true
    fields:
      log_type: audit
      cluster: xinference
    fields_under_root: true

processors:
  - drop_fields:
      fields:
        - agent
        - ecs
        - host
        - input
        - log
        - event
        - data_stream
        - elastic_agent
        - cloud
        - container
        - kubernetes
        - orchestrator
        - "@version"
      ignore_missing: true

  - timestamp:
      field: "@timestamp"
      layouts:
        - '2006-01-02T15:04:05.000Z'
      timezone: "UTC"
      ignore_failure: true

output.elasticsearch:
  hosts: ["http://elasticsearch:9200"]
  indices:
    - index: "xinference-audit-%{+yyyy.MM.dd}"
      when.equals:
        log_type: "audit"
    - index: "xinference-logs-%{[log_type]}-%{+yyyy.MM.dd}"
      when.has_fields: ["log_type"]
  # username: "elastic"
  # password: "changeme"

setup.template.name: "xinference"
setup.template.pattern: "xinference-*"
setup.template.settings:
  index.number_of_shards: 2
  index.number_of_replicas: 1

setup.ilm.enabled: false
setup.ilm.rollover_alias: "xinference-logs"
setup.ilm.policy_name: "xinference-logs-policy"

logging.metrics.enabled: true
logging.metrics.period: 60s
logging.level: info
logging.to_files: true
logging.files:
  path: /var/log/filebeat
  name: filebeat
  keepfiles: 7
  permissions: 0644
