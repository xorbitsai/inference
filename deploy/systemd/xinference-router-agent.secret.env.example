# Dedicated credential used by Router Agent and its Runtime children.
# Keep this file root:cloud 0640 and use the same value on Supervisor.
XINFERENCE_TOKEN_ROUTER_INTERNAL_TOKEN=<dedicated-random-token>
